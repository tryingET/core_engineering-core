---
summary: "Agent and operator migration map for engineering-core v0.2.0 breaking rename."
read_when:
  - "Migrating consumers, prompts, templates, fixtures, or scripts to engineering-core v0.2.0."
type: "migration-map"
---

# Migration map — engineering-core v0.2.0

## Applies to

Consumers of the earlier shared lane package, CLI, file-prefix, repo-local override, or policy metadata names.

## Required replacements

- Use repo path `/home/tryinget/ai-society/core/engineering-core`.
- Use Python package/import path `engineering_core`.
- Use CLI command `engineering-core`.
- Use lane files named `engineering-<lane>.md` and addenda named `engineering-<lane>.<addendum>.md`.
- Use repo-local override file `docs/engineering.local.md`.
- Use machine-readable policy file `policy/engineering-lane.json`.

## Consumer update checklist

1. Update prompts, skills, docs, fixtures, generated templates, and operator scripts to the new names.
2. Remove compatibility aliases instead of forwarding old commands or import paths.
3. For frontend/browser apps, select `ts` plus `ts-frontend` rather than using the frontend addendum alone.
4. Select only the discipline docs that own the current concern.
5. Record local deviations in `docs/engineering.local.md`.

## Validation

From `engineering-core`:

```bash
python scripts/release-local.py verify --version 0.2.0
```

For downstream consumers, run their repo-local docs strict and standard check surface after updating references.

## Evergreen-doc rule

Do not copy this version-specific migration map into first-contact docs. Keep evergreen docs focused on current engineering-core usage.
