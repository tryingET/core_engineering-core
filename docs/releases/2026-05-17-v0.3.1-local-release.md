---
summary: "Local release notes for engineering-core v0.3.1."
read_when:
  - "Verifying or tagging engineering-core v0.3.1."
type: "draft"
---

# Local release draft — engineering-core v0.3.1

## Title

`engineering-core v0.3.1 — release-history clarification`

## Release body

This patch release clarifies the release narrative after session-log review. `v0.3.0` did not only add the 2,000 ft discipline layer; because it was cut from current `HEAD`, it also included the previously completed 10,000 ft authority/adoption/lifecycle foundation and the 5,000 ft CLI/catalog/template product surface.

### Breaking Changes

- None.

### Added

- No new runtime or guidance surfaces.

### Changed

- Clarified `CHANGELOG.md` so the release history accurately describes `v0.3.0` as the first release containing the full 10,000 ft + 5,000 ft + 2,000 ft engineering-core stack.
- Bumped package metadata to `0.3.1` for an annotated local patch tag.

### Fixed

- Fixed release narrative drift caused by describing `v0.3.0` too narrowly as only the 2,000 ft discipline-completion release.

### Agent migration notes

- No migration required. Consumers pinned to `v0.3.0` already have the same 10,000 ft, 5,000 ft, and 2,000 ft content. Pin to `v0.3.1` if you want the corrected release-history metadata.

### Verification before tag

```bash
python scripts/release-local.py verify --version 0.3.1
```

### Build artifact

```bash
uv build
```

`dist/` artifacts are generated proof output and are not committed.
