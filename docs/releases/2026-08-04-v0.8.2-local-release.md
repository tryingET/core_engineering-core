---
summary: "Local engineering-core v0.8.2 release-candidate note for the bounded advisory agent-interaction profile."
read_when:
  - "Preparing, verifying, or evaluating the local engineering-core v0.8.2 agent-interaction release candidate."
type: "release-note"
---

# Engineering-core v0.8.2 agent-interaction local release candidate

## Title

Engineering-core v0.8.2 candidate — bounded advisory agent-interaction profile

## Release body

The local v0.8.2 candidate adds the smallest reversible `agent-interaction` catalog profile. It presets no language lane and recommends only existing disciplines, in evidence-grounded order:

1. `validation`
2. `testing`
3. `security-privacy`
4. `observability`
5. `local-first-data`
6. `specification-and-dsls`
7. `documentation`

Agent Kernel task 4667 is the execution membrane for this scoped change. The candidate catalog addition follows bounded cross-owner G3 read-only canary evidence with an independent PASS. That evidence supports candidate catalog inclusion only: it does not prove consumer adoption or runtime effectiveness, and this preparation does not claim a completed/tagged release or publication, define ontology, or impose mandatory policy.

The profile remains advisory. Consumers retain ownership of lane selection, local deviations, validation, and adoption decisions.

### Breaking Changes

None. No discipline, lane, runtime integration, or enforcement surface is added.

### Verification before tag

```bash
python -m py_compile src/engineering_core/cli.py src/engineering_core/adoption_scan.py src/engineering_core/adoption_render.py
python -m unittest discover -s tests
python scripts/check-justfile-addenda.py
uv run engineering-core list
uv run engineering-core catalog --pretty --prefer-repo
uv run engineering-core scan-adoption --scope /home/tryinget/ai-society/core --repo-discovery recursive --include-scope-root --include-packages --format json --prefer-repo
uv run engineering-core recommend agent-interaction --prefer-repo
uv build
python scripts/release-local.py plan --version 0.8.2
python scripts/release-local.py verify --version 0.8.2
```

This is a local release candidate and verification record only. No completed/tagged release, registry publication, push, or adoption action is performed.
