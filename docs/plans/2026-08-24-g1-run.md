---
summary: "G1 disposable-replica journeys against candidate-2 b313bec. Producer-run; not G1 PASS."
read_when:
  - "Reading G1 journey results or deciding whether Gate G1 has passed."
type: "implementation-plan"
---

# G1 run (no PASS)

Candidate wheel from `b313bec`. Each positive baseline is cloned into an
isolated disposable replica (`git clone --no-hardlinks`). The candidate is
the installed wheel, not an editable checkout.

Public surfaces used: `init` (plan/apply), `plan`, `doctor`, `list`/`show`,
`scan-adoption`. There is **no** candidate-shipped rollback command; those
journeys are `incomplete`. Independent review is **not** claimed.

HealthCo remains an untouched negative control (no replica apply).
