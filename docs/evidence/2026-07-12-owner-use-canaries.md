---
summary: "Operator-attested v0.8 canary manifest over the three most recently committed physical Git roots under softwareco/owned."
read_when:
  - "Reviewing whether v0.8 owner-use packets were useful on current owned-repository work."
  - "Auditing real matched/stale behavior, task-bound requests, or owner handoff limits."
type: "evidence"
---

# Engineering-core v0.8 real owner-use canaries

## Selection receipt

A dated selection projection deduplicated current AK-registered paths under `softwareco/owned` by physical Git root, then ranked 37 eligible roots by latest commit timestamp. The top three were:

1. `agent-kernel` — AK task `3802`, completed 21-token owner-fact preflight;
2. `dspx` — AK task `3801`, completed conversational quality-criterion implementation;
3. `pi-extensions` — pending AK task `3427`, package loop-validation adoption inside a dirty monorepo.

The machine artifact retains capture time, exact read-only commands, the top-ten ranking, commit identities, dirty-path counts, bounded AK task projections, and a manifest self-digest. This remains an operator-attested routing projection, not canonical repository-activity or task authority; AK and each repository remain authoritative.

## Product results

| Repository | Mode | Task-bound value | Bundle posture | Final verification |
|---|---|---|---|---|
| `softwareco/owned/agent-kernel` | advisor-ready | objective, required paths, source evidence, bounded plan, and two preflight recommendations | two pending owner reviews | matched |
| `softwareco/owned/dspx` | advisor-ready | current dirty implementation bytes, bounded plan, and two offline/evidence-boundary recommendations | two pending owner reviews | matched after refresh |
| `softwareco/owned/pi-extensions` | plan-only | representative package-local task scope without absorbing unrelated dirty work | prepared; no advice | matched |

The task-bound requests now include owner objective, focus paths, constraints, validation expectations, scope/revision bindings, and redacted focus-file evidence. Advice cannot be replayed across different task contexts in the same repository because the request digest changes with context and scope.

DSPx changed focused dirty files between one preparation and verification during development. `verify-work` reported `stale`, with both scope and task-bound request bindings drifted. That observation predates the final catalog digest and is retained only as behavioral evidence. After the catalog froze, all three final packets were re-prepared from current owner bytes and verified `matched`; the machine artifact retains both the earlier stale output and final matched outputs.

## Owner handoff result

- `summarize-work` produced concise Markdown handoffs with pending recommendations, focus paths, validation expectations, drift, and next authority.
- The pi-extensions peer explicitly acknowledged the plan-only/non-authoritative boundary and declined mutation because its current work belonged to a separate architecture discussion.
- Agent Kernel and DSPx owner-session disposition requests received no reply before capture.
- Therefore owner dispositions and receipts remain truthfully **0 / 0**; four recommendations remain **pending owner review** rather than being fabricated or promoted.

## Boundaries

- Engineering-core invoked **zero models**.
- The current operator session supplied four external advisory recommendations.
- Engineering-core executed **zero consumer commands** and applied **zero patches**. Fixed Git probes disable fsmonitor, pathspec magic, untracked-cache integration, and optional locks.
- No AK, CI, release, compliance, approval, doctrine, or rollout authority was promoted.
- Full packets/bundles remain local because they contain bounded owner source excerpts. Checked-in evidence retains task context, evidence manifests without content, digests, recommendation text, bundle records, and complete verification outputs.

Machine-readable operator-attested manifest: `docs/evidence/2026-07-12-owner-use-canaries.json`.
