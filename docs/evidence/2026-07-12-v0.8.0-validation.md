---
summary: "Validation evidence for the local engineering-core v0.8.0 owner-use workflow release."
read_when:
  - "Auditing v0.8.0 release proof, adversarial review, or real owner-use canaries."
type: "evidence"
---

# Engineering-core v0.8.0 validation

## Release identity

- Release commit: `8f59f4178f0c40f73d64c417e7a591de42a0f0d2`
- Annotated tag: `v0.8.0`
- Local self-adoption commit: `19a525d`
- Publication: local Git tag and local wheel/sdist only; no registry publication

## Automated verification

`python scripts/release-local.py verify --version 0.8.0` passed before and after the release commit. It covered:

- package/catalog/version consistency;
- v0.6 and v0.7 historical catalog identity;
- Python compilation and Justfile addendum validation;
- closed-loop, capability, reconciliation, and owner-use dogfoods;
- **127 unit tests**;
- CLI discovery and bounded scanner smoke checks;
- wheel and sdist build/content inspection.

Strict repository docs validation and `git diff --check` passed. Every touched code file remains below 500 lines.

The deterministic owner-use dogfood produced byte-identical output across repeated runs and proved matched/stale verification, exact external advice/disposition/receipt joins, cross-repository rejection, inert patch posture, and zero consumer command/model/authority effects.

## Defensive and independent review

Independent reviewers initially found and caused correction of:

- repository-configured fsmonitor execution and optional Git metadata writes;
- incompatible packet/bundle round-trip byte limits;
- shallow re-digested nested schema validation;
- task context absent from advisor requests;
- hidden pending recommendations and weak owner handoff UX;
- historical v0.7 protocol projection;
- non-finite and oversized re-digested packet acceptance;
- incoherent/nonhex/oversized verification acceptance;
- plan-only omission of constraints and stop conditions.

The final release-gate review returned **PASS** after adversarial probes rejected request authority/revision tampering, snapshot ceiling changes, duplicate or invalid plan selections, non-finite/oversized packets, and incoherent/nonhex/oversized verification objects. Package Git reads now route through the sanitized `safe_git` helper; release read probes use equivalent controls.

## Real owned-repository canaries

The dated canary manifest selected the three most recently committed, AK-registered physical Git roots under `softwareco/owned`:

- Agent Kernel task `3802` — advisor-ready;
- DSPx task `3801` — advisor-ready;
- pi-extensions task `3427` — plan-only.

All final packets verified `matched`. Four externally supplied recommendations remain truthfully pending owner review; owner dispositions and receipts remain zero rather than being fabricated. A development DSPx packet correctly became stale during live focused-file changes and matched after re-preparation.

See `docs/evidence/2026-07-12-owner-use-canaries.json` and `.md`.

## Authority boundary

No engineering-core command invoked a model, executed a consumer command, applied a patch, opened receipt observation references during finalization, mutated AK, or promoted CI, release, compliance, approval, doctrine, or rollout authority.
